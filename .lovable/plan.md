

## Plan: Rename "reports" to "edge lab" in sidebar

**Change**: Update the label in the `workspaceItems` array in `src/components/AppNavSidebar.tsx` from `"reports"` to `"edge lab"`.

Single line change — the `href` (`/app`) and icon (`BarChart3`) stay the same.

